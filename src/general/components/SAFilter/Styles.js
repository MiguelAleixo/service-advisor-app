import styled from 'styled-components';

export const FilterMargin = styled.View`
    margin: 6px 0;
    display: flex;
    flex-direction: row;
    align-items: center;
`;

export const style = {
    label: {
        color: '#212121',
        fontSize: 16,
        fontWeight: 'bold'
    },
    filter: {
        backgroundColor: '#FFF',
        height: 28,
        width: '100%'
    }
}